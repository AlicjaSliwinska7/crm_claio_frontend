// Statusy zamówień (używane w kilku miejscach)
export const ORDER_STATUSES = ['w przygotowaniu', 'wysłane', 'podpisane', 'zarejestrowane']

// Zmiany z harmonogramu (jeśli używasz skrótów 1/2/3/u/l)
export const SHIFT_TYPES = ['1', '2', '3', 'u', 'l']

// Kolory znaczników/priorów – trzymane w jednym miejscu
export const PRIORITY_COLORS = {
  low:    'var(--color-ok, #2e7d32)',
  medium: 'var(--color-warn, #f9a825)',
  high:   'var(--color-danger, #c62828)',
}
